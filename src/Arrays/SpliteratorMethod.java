package Arrays;

import java.util.Arrays;

//The method is used to create a Spliterator from the given array
public class SpliteratorMethod {
    public static void main(String[]args){
        //Initialize the array
        int[]intArray = {90, 20, 80, 70, 50, 60, 40, 30, 10};
        //Sort the array using the normal sort
      System.out.println("Sorted Array: " + Arrays.spliterator(intArray));

    }
}
