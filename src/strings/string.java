package strings;

class string {
    public static void main(String[]args){
        String name = "A String is an object that stores a sequence of characters";
        String str = new String("An array of characters");
        System.out.println("A String is: " + str);
        System.out.println("Strings are immutable and: " + name);
    }
}
